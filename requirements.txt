# NEMO-OS System Requirements
# Core dependencies

# Security: Runtime validation
Pydantic>=2.0

# Service: Platform integration (Windows optional)
psutil>=6.0

# Bus: Binary serialization (optional, falls back to JSON)
msgpack>=1.0

# Action Executor: OS automation
pyautogui>=0.9.53
mss>=9.0.1
pygetwindow>=0.0.9
Pillow>=10.0.0

# Chrome Profile Detection: OCR for automatic profile selection
easyocr>=1.7.0
opencv-python>=4.8.0

# Clipboard: Multi-line code typing
pyperclip>=1.8.2

# AI/Vision/Voice: Ollama, Requests
requests>=2.31.0
RealtimeSTT>=0.0.23

# Vision: OmniParser (AI-powered UI element detection)
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.30.0
timm>=0.9.0
einops>=0.7.0

# Voice: faster-whisper for speech recognition
faster-whisper>=0.10.0
sounddevice>=0.4.6
numpy>=1.24.0

# Browser: Playwright for web automation
playwright>=1.40.0
nest-asyncio>=1.5.8

# Web: Flask for OpenClaw bridge
Flask>=3.0.0

# Optional: Uncomment for Windows service support
# pywin32>=305
